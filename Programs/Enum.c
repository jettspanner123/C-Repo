#include <stdio.h>

typedef enum
{
    Monday = 20,
    Tuesday = 10,
    Wednesday = 30,
    Thursday = 40,
    Friday = 50,
    Saturday = 70,
    Sunday = 30
} Days;

int main(void)
{
    printf("%d\n", Monday);
}
